name: dio_cache_ttl
description: Caching system for Flutter using dio with TTL.
version: 0.3.1
homepage: https://github.com/cas8398/dio_cache_ttl
repository: https://github.com/cas8398/dio_cache_ttl
issue_tracker: https://github.com/cas8398/dio_cache_ttl/issues
documentation: https://github.com/cas8398/dio_cache_ttl

environment:
  sdk: ">3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0+1
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mockito: ^5.4.5
  build_runner: ^2.4.15
  http_mock_adapter: ^0.6.1
  mocktail: ^1.0.4

topics:
  - cache
  - ttl
  - dio
  - file

# The following section is specific to Flutter packages.
flutter:
  generate: true
